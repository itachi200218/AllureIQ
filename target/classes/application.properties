# ? MongoDB connection (same for local and Render)
spring.data.mongodb.uri=mongodb+srv://chetanyaadepu:Chetan%400903@cluster0.onayhld.mongodb.net/LuffyFramework?retryWrites=true&w=majority&appName=Cluster0

# ? Use Render's dynamic port if available, otherwise default to local
server.port=${PORT:8081}

# (Optional but good)
spring.main.allow-bean-definition-overriding=true
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
